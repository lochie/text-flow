{
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "pnpm -r install",
    "watch": "pnpm --filter=content-transition dev",
    "dev": "pnpm run watch --onSuccess \"pnpm -w run site:dev\"",
    "build": "pnpm --filter=content-transition build",
    "site:dev": "pnpm --filter=site dev",
    "site:build": "pnpm run build && pnpm --filter=site build",
    "lint": "pnpm --filter=content-transition lint",
    "lint:fix": "pnpm --filter=content-transition lint:fix",
    "pre-commit": "pnpm run --filter=content-transition pre-commit",
    "version": "changeset version && git add --all",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "@commitlint/cli": "^17.8.0",
    "@commitlint/config-conventional": "^17.8.0",
    "lint-staged": "^15.0.2"
  }
}
